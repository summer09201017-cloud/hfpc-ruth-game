// 「盼望 · 以利亞重得力」動作關所有可調整的數值集中在這裡,方便平衡與微調。
// 這是 src/minigames/ 下自成一體的引擎(仿 sling/),約拿 fork 之外——sync:jonah 不會碰它。
// 跑酷物理沿用約拿引擎的數值;玩法核心改為「收集餅水恢復體力」(王上 19:5–8)。

// 邏輯解析度(實際畫面會等比縮放填滿視窗,並做黑邊置中)
export const VIEW = { W: 960, H: 540 }

// 地面基準線(以利亞的腳底站在這裡)
export const GROUND_Y = VIEW.H - 96

// 物理(跳起來撿懸空的餅水)
export const PHYS = {
  gravity: 2200, // 重力加速度 (px/s^2)
  jumpV: -780, // 起跳初速度 (px/s,負值=向上)
}

// 玩家(以利亞):固定在畫面左側,世界往左捲動製造前進感
export const PLAYER = {
  x: 190,
  w: 40, // 命中框寬
  h: 58, // 命中框高
}

// 跑酷節奏:以利亞是灰心、疲乏的,所以比約拿慢一些
export const RUN = {
  startSpeed: 250, // 起始捲動速度 (px/s)
  maxSpeed: 340, // 最高速度
  rampDistance: 6000, // 在此距離內線性加速到最高速
  goalDistance: 8000, // 跑完這段曠野 = 抵達何烈山
}

// 體力(恢復系統):這一關的核心。前進緩慢消耗,撿餅🍞水💧回補。
// 歸零不會失敗——以利亞癱坐片刻,神再扶他起來(王上 19:7),原地小補後續走。
export const STAMINA = {
  max: 100,
  start: 45, // 起始偏低(剛求死、灰心)
  drainPerSec: 7, // 前進時每秒消耗
  bread: 24, // 撿到餅🍞回補
  water: 13, // 撿到水💧回補
  hazardCost: 12, // 撞到熱浪/塵霧只小扣體力(不致命)
  faintRefill: 30, // 癱坐後神再扶起時回補到的體力
  low: 28, // 低於此值時體力條轉紅、提示「該吃點東西了」
}

// 癱坐(體力歸零):溫柔的停頓,不是失敗
export const FAINT = {
  duration: 1.6, // 癱坐動畫秒數(之後自動再起)
}

// 「炭火燒的餅」特殊大補(對應「仗著這飲食的力,走了四十晝夜」王上 19:8):
// 遇見天使領受時把體力補滿 + 短暫加速。
export const BOOST = {
  duration: 4, // 加速持續秒數
  mult: 1.4, // 速度倍率
}

// 天使(王上 19:5–7「天使…說:起來吃吧」):一路上「常常遇到天使」,
// 走到天使面前才停下、出現對話(天使的話+經文),天使供應餅🍞水💧恢復體力,再前行。
export const ANGEL = {
  firstAt: 560, // 第一位天使很快出現(開場不久就遇見)
  gapMin: 1050, // 天使間最小距離(要「常常」遇到 → 間距小)
  gapMax: 1500, // 最大距離
  refill: 62, // 領受餅水恢復的體力
  reach: 30, // 天使走到離以利亞這麼近 = 觸發對話
}

// 可選「恢復門檻」:抵達何烈山前需累計吃喝足夠(呼應「仗著飲食的力」)。
// 預設 0 = 關閉(維持溫柔,走到即過關);想加難度可調成例如 60。
export const NEED_MEAL = 0
